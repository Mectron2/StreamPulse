global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: aggregator
    metrics_path: /metrics
    static_configs:
      - targets: ['aggregator:3001']
        labels:
          service: aggregator

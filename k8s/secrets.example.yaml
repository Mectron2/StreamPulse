# Copy this file to a non-versioned file, replace every value, then apply it.
apiVersion: v1
kind: Secret
metadata:
  name: stream-pulse-secrets
  namespace: stream-pulse
type: Opaque
stringData:
  rabbitmq-url: amqp://guest:guest@rabbitmq:5672
  postgres-password: REPLACE_ME
  grafana-admin-user: admin
  grafana-admin-password: REPLACE_ME
